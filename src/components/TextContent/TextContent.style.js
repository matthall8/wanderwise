import styled from "styled-components";

export const StyledText = styled.p` {
    font-size: 1rem;
    font-family: 'Muli', sans-serif;
    padding: 12px 16px;
    margin: 0px auto;
    text-align: left;
    line-height: 1.75;
    width: 40em;
    font-weight: normal;
  }
  `;